<%= div_for comment do %>
    <div id="comment" class="comment_<%=comment.id%> clearfix">
      <%= link_to image_tag(comment.user.avatar(:thumb)), comment.user, class: 'comment_avatar' %>
      <div id="comment_content"><%= render 'comments/content', comment: comment %></div>
    </div>
<% end %>
